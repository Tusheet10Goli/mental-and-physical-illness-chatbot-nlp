CREATE TABLE users (
    user_id varchar(64),
    fname varchar(256),
    mname varchar(256),
    lname varchar(256),
    dob varchar(10),
    gender varchar(1),
    email varchar(256),
    password varchar(64)
);
